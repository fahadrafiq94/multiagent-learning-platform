services:
  backend:
    build:
      context: ../..
      dockerfile: docker/backend/Dockerfile

    container_name: multiagent-prod-backend

    ports:
      - "8000:8000"

    env_file:
      - ../../backend/.env

    volumes:
      - ../../backend/app:/app/app

    restart: unless-stopped